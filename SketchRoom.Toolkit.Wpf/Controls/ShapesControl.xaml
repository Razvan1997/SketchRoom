<UserControl
    x:Class="SketchRoom.Toolkit.Wpf.Controls.ShapesControl"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:converters="clr-namespace:SketchRoom.Toolkit.Wpf.Converters"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:SketchRoom.Toolkit.Wpf.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:svgc="http://sharpvectors.codeplex.com/svgc/"
    x:Name="Root"
    d:DesignHeight="450"
    d:DesignWidth="800"
    mc:Ignorable="d">

    <UserControl.Resources>
        <converters:NullToVisibilityConverter x:Key="NullToVisibilityConverter" />
        <converters:InverseNullToVisibilityConverter x:Key="InverseNullToVisibilityConverter" />
    </UserControl.Resources>

    <StackPanel>
        <TextBox Margin="0,0,0,10" Text="{Binding SearchText, ElementName=Root, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

        <ItemsControl ItemsSource="{Binding ItemsSource, ElementName=Root}">
            <ItemsControl.ItemsPanel>
                <ItemsPanelTemplate>
                    <WrapPanel Orientation="Horizontal" />
                </ItemsPanelTemplate>
            </ItemsControl.ItemsPanel>

            <ItemsControl.ItemTemplate>
                <DataTemplate>
                    <Border
                        Width="80"
                        Height="80"
                        Margin="5"
                        Background="White"
                        BorderBrush="Gray"
                        BorderThickness="1"
                        CornerRadius="5"
                        PreviewMouseDown="OnShapeMouseDown"
                        PreviewMouseMove="OnShapeMouseMove"
                        PreviewMouseUp="OnShapeMouseUp">
                        <Viewbox>
                            <Grid>
                                <ContentControl Content="{Binding ShapeContent}" Visibility="{Binding ShapeContent, Converter={StaticResource NullToVisibilityConverter}}" />
                                <svgc:SvgViewbox Source="{Binding SvgUri}" Visibility="{Binding ShapeContent, Converter={StaticResource InverseNullToVisibilityConverter}}" />
                            </Grid>
                        </Viewbox>
                    </Border>
                </DataTemplate>
            </ItemsControl.ItemTemplate>
        </ItemsControl>
    </StackPanel>
</UserControl>
