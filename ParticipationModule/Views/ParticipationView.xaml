<UserControl
    x:Class="ParticipationModule.Views.ParticipationView"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:controls="clr-namespace:SketchRoom.Toolkit.Wpf.Controls;assembly=SketchRoom.Toolkit.Wpf"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:local="clr-namespace:ParticipationModule.Views"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:prism="http://prismlibrary.com/"
    d:DesignHeight="450"
    d:DesignWidth="800"
    prism:ViewModelLocator.AutoWireViewModel="True"
    mc:Ignorable="d">
    <Grid>

        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <controls:CardControl
            Grid.Row="0"
            Width="300"
            Margin="10"
            HorizontalAlignment="Center"
            Background="#f0f0f0">
            <StackPanel>
                <TextBlock
                    Margin="0,0,0,5"
                    FontSize="16"
                    FontWeight="Bold"
                    Text="Participate to an in live sketch room" />
                <TextBlock Text="Enter here the invite code." TextWrapping="Wrap" />
                <TextBox Text="{Binding SessionCode}" />
                <controls:ActionButton
                    Margin="0,10,0,0"
                    ButtonCommand="{Binding StartParticipationCommand}"
                    ButtonText="Participate"
                    IsButtonEnabled="{Binding IsStartParticipationEnabled}" />
            </StackPanel>
        </controls:CardControl>
    </Grid>
</UserControl>
